# Character-based embedding language modeling.
# Create word embedding based of neighboring word embeddings
# Based off of ELMo
# Demo has an embedding for each word in each sentence. This is stored by id,
# and the id is stored in a seperate database

